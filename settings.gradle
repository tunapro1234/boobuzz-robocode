pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

include ':FtcRobotController'
include ':TeamCode'

// Constitution rule 1: :core and :sim must not see the FTC SDK.
// The rule is enforced as a compile error -> gradle/sdk-guard.gradle
include ':core'
project(':core').projectDir = file('TeamCode/core')
include ':sim'
