package boobuzz.core.contract;

/** Edge-triggered request types. */
public enum RequestType {
    SHOOT,
    INTAKE,
    GOTO,
    PATH,
    SPIN_UP,
    INTAKE_ON,
    INTAKE_OFF,
    TURN_TO,
    TURRET_AIM,
    RESET_POSE,
    SWITCH_ENGINE
}
